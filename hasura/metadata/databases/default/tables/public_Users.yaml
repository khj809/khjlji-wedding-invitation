table:
  name: Users
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - name
        - provider
      filter: {}
  - role: user
    permission:
      columns:
        - id
        - name
        - provider
      filter: {}
      query_root_fields:
        - select_by_pk
      subscription_root_fields: []
